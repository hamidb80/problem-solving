import std/[unittest]
import knapsack/[dynamic, greedy, backtracking]

func 

suite "correctness":
  let items: seq[Item] = @[
    newItem(50, 5),
    newItem(60, 10),
    newItem(140, 20),
  ]

  dynamic.

1_000_000_000